定时读取摄像头帧，默认每 5s 一次。
启动时只加载一次 YOLO 模型，避免每轮重复加载。
检测到目标后读取 x_center。
调用 stepper_control.py 里的线性映射函数，默认 y = x，得到映射步数。
启动时打开一次步进电机开发板串口，循环内复用连接，避免 CH340/Arduino 反复串口重连导致开发板重启。
默认只在目标步数变化超过 --min-step-delta 时发送新命令，避免静态目标下重复累计运动。
支持运行时标定映射：--map-slope 和 --map-intercept，即 y = kx + b。
## 运行示例：

conda activate torch_env
python control.py --camera-index 1 --conf 0.1 --port COM3 --baud 9600
如果想明确用 GPU：

python control.py --camera-index 1 --conf 0.1 --device 0 --port COM3 --baud 9600
如果要临时改映射，例如 y = 2x + 10：

python control.py --camera-index 1 --port COM3 --map-slope 2 --map-intercept 10
我已验证：

conda activate torch_env; python -m py_compile control.py detect_loop.py stepper_control.py
conda activate torch_env; python control.py --help
conda activate torch_env; python control.py --once --dry-run --camera-index 1 --conf 0.1 --map-slope 1 --map-intercept 0
dry-run 能正常加载模型、打开摄像头并取帧；这次画面里未检测到目标，所以没有触发步进命令。当前 python control.py --list-ports 返回 No serial ports found，如果实际测试时开发板已插入但仍这样显示，优先检查 USB 连接、Arduino IDE 串口监视器是否占用、CH340 驱动和端口号。

注意步进电机端 Arduino sketch 需要接收一行整数命令，例如 320\n，并建议回 OK:320 或 ERR:...；Python 侧已经按这个协议等待回执。